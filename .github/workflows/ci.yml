name: CI

on:
  push:
    branches: [main, "phase-*"]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # --- Correctness -----------------------------------------------------
      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test

      # --- Design system ---------------------------------------------------
      # Tokens are generated from lib/design/tokens.ts. If the committed CSS is
      # stale, the TypeScript and the CSS have diverged and the build is lying
      # about which one is the source.
      - name: Design tokens are current
        run: pnpm design:tokens:check

      # Arbitrary Tailwind values, prohibited typefaces, glassmorphism,
      # decorative gradients, malformed colour utilities, faded text colours,
      # oversized radii.
      - name: Design system rules
        run: pnpm design:check

      # --- Build -----------------------------------------------------------
      # The seven evidence-integrity rules run inside the content loader, so a
      # violation fails here rather than shipping an unlabelled claim.
      - name: Build
        run: pnpm build

      - name: Bundle budget
        run: pnpm budget:check

  accessibility:
    runs-on: ubuntu-latest
    needs: verify
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm build

      # Every route, both surfaces, against the production build. This is the
      # only gate that sees what the renderer actually produced — the token
      # contrast assertions cannot tell whether a page uses the pairings they
      # assert, or whether a class name resolved to any CSS at all.
      - name: axe on every route
        run: pnpm test:a11y
